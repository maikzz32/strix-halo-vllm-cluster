# Qwen3.8 Flash Next: vLLM 0.29 on nodes 1 and 2

Endpoint: http://192.168.1.15:8000/v1

Model ID: `/home/maik/qwen38_rest`. Both hosts use container `qwen029-tp2`, TP2, native MP, MTP3, original asymmetric INT4 group32 weights and a 262144-token context limit. The TP4-specific UMA/W1/W2 flags are disabled. Both live workers opened `/dev/infiniband/uverbs3` for RCCL communication.

The custom version `0.29.0+strix.rocm100` incorporates the complete eight-file runtime delta from upstream commit33898f832 to release98dff2a81d74, preserving the Strix patches. Both nodes have matching hashes for these files. The upstream delta changes no native extension sources or dependencies, so the existing gfx1151 Torch2.13/ROCm10 extensions are retained. This is a release-based custom installation, not the stock ROCm7.2.3 wheel. Version metadata and RECORD hashes were updated. Overlayfs refused a dist-info directory rename, so the original directory name remains; that failure did not change serving code.

The old stopped ray-head/ray-worker containers and complete pre-upgrade images are preserved. Final images are `localhost/strix-vllm:v0.29.0-strix-rocm100-node1` and `localhost/strix-vllm:v0.29.0-strix-rocm100-node2`. Nodes3/4 were not modified.

Run95621cf6e94b4528a9aad45bcba563dc reached READY. Weight memory87.31GiB/GPU; KV memory7.1GiB and capacity476110tokens, approximately1.82 maximum-length contexts. Three256-token C1 requests and six128-token C2 requests succeeded with coherent text. The final success counter was9 with empty queues. This is a functional check, not a ShareGPT48 speed comparison or proof of output parity with TP4.

Config on both hosts: `/home/maik/strix-halo-next/config/qwen029-tp2.json`.

On node1, use `python3 /home/maik/strix-halo-next/tools/cluster.py status --config /home/maik/strix-halo-next/config/qwen029-tp2.json`. Replace `status` with `stop` to stop serving. To start, use `start --config /home/maik/strix-halo-next/config/qwen029-tp2.json --tag qwen029-tp2 --timeout 900`.

After a host reboot, first start `qwen029-tp2` with Podman on both hosts, then use the controller start command on node1. No boot or crash supervisor was added.

Local evidence is under `research/vllm029-tp2` and `results/qwen029-tp2-c1-20260909.json` / `results/qwen029-tp2-c2-20260909.json`. Upstream comparison: https://github.com/vllm-project/vllm/compare/33898f832...v0.29.0

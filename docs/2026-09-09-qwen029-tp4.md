# Qwen Flash Next: vLLM 0.29 TP4

Run `a19e8f8dc60a48aeab3fdbe8e35d71e0` serves all four nodes at
`http://192.168.1.15:8000/v1`, model `/home/cluster-user/qwen38_rest`.
Node 3's `qwen` llama.cpp container was stopped as requested; weights remain.

The custom `0.29.0+strix.rocm100` installation preserves the original INT4
GS32 weights and Strix kernels. TP4 uses MTP3, FULL_DECODE_ONLY, 262144 context,
and the validated W1 expert order, W2 serial and AVX512 UMA communication.
Hermes auto tool calls use `--enable-auto-tool-choice --tool-call-parser qwen3_xml`.

All four hosts now use `/home/cluster-user/strix-halo-next/config/cluster.json`, identical
to `config/qwen029-tp4.json` (SHA256
`0e617e5ac6d2365ed7f6eba123d3d0aa77eaffbb88239be7507b8966ad29e369`).
Containers on nodes 1/2 retain the historical name `qwen029-tp2`; nodes 3/4 use
`qwen029-tp4`. These names do not indicate the active tensor parallel size.

Use the native controller on node 1 with the canonical config for status,
start or stop. The old TP2 config covers only two hosts and must not be used
to manage this four-node service. No boot/crash supervisor was added.

All four workers passed fresh eager/graph UMA parity against PyNccl. Auto
tool calls, streamed tool calls, tool-result roundtrip and default thinking
passed. Six concurrent-pair requests of 128 tokens passed. These are functional
tests, not a new throughput benchmark. Evidence is in
`bench/records/2026-09-09-qwen029-tp4.json`.

Pre-upgrade and final custom images are preserved on each node. Final images
are `localhost/strix-vllm:v0.29.0-strix-rocm100-nodeN` for N=1..4.
